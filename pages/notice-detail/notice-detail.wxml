<view class="card" wx:if="{{item}}">
  <view class="title">{{item.title}}</view>
  <view class="muted" style="margin-top:10rpx;">{{item.publishTime}} · {{item.department}}</view>
  <view style="margin-top:16rpx; line-height:1.7;">{{item.content}}</view>
</view>